# Presentation Service in Kotlin

Build and run:
```shell
./gradlew run -P:htmlPath=(path to deck.html)
```

Build then run:
```shell
./gradlew build
java -jar build/libs/presentation-service-all.jar -P:htmlPath=(path to deck.html)
```
